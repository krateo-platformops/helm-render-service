apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-render-service.fullname" . }}
  labels:
    {{- include "helm-render-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-render-service.selectorLabels" . | nindent 4 }}
