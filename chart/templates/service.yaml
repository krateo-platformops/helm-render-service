apiVersion: v1
kind: Service
metadata:
  name: {{ include "krateo-helm-render-service.fullname" . }}
  labels:
    {{- include "krateo-helm-render-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krateo-helm-render-service.selectorLabels" . | nindent 4 }}
